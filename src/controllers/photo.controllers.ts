import { Request, Response } from "express";
import PhotoModel, { IPhoto } from "../models/photo.model";
import fs from "fs-extra";
import path from "path";
class PhotoController {
  
}

export const PhotoRoutes = new PhotoController();
